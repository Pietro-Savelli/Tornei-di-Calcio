# Variabili d'ambiente per il frontend React (Vite)
# Vite espone solo le variabili che iniziano con VITE_ al client-side.

# Base URL del backend (lascia vuoto per usare path relativi)
VITE_API_BASE_URL=

# Esempio per sviluppo con backend separato:
# VITE_API_BASE_URL=http://localhost:8080
